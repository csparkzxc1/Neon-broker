{
  "name": "neon-broker",
  "version": "0.1.0",
  "description": "NEON BROKER — solo build prototype (market simulation only).",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "python3 serve.py",
    "smoke": "node src/_smoke.mjs"
  }
}
